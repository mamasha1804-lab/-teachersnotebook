function renderAuth(app){const accounts=getAccounts();if(!Object.keys(accounts).length)authMode='register';app.innerHTML=`<div class="auth-screen"><section class="auth-card"><div class="auth-logo">TEACHER <span class="mini-heart">♡</span> DAY</div><div class="auth-sub">Личный ежедневник учителя</div>${authError?`<div class="auth-error">${esc(authError)}</div>`:''}<div class="auth-tabs"><button class="auth-tab ${authMode==='login'?'active':''}" data-auth-tab="login">Вход</button><button class="auth-tab ${authMode==='register'?'active':''}" data-auth-tab="register">Создать доступ</button></div><form class="auth-fields" data-auth-form>${authMode==='register'?`<label class="auth-field"><span>Имя учителя</span><input id="authName" autocomplete="name" placeholder="Например, Лариса" required></label>`:''}<label class="auth-field"><span>Логин</span><input id="authLogin" autocomplete="username" placeholder="Ваш логин" required></label><label class="auth-field"><span>Пароль</span><input id="authPassword" type="password" autocomplete="${authMode==='login'?'current-password':'new-password'}" minlength="4" placeholder="Пароль" required></label><button class="auth-submit" type="submit">${authMode==='login'?'Войти':'Создать аккаунт'}</button></form><div class="auth-note">Имя учителя будет отображаться в приветствии. В этой автономной версии данные и пароль хранятся только в вашем браузере. Перед публичным размещением на GitHub для настоящей защищённой авторизации подключим серверную авторизацию.</div></section></div>`;bindAuth()}
function bindAuth(){$$('[data-auth-tab]').forEach(b=>b.onclick=()=>{authMode=b.dataset.authTab;authError='';render()});$('[data-auth-form]').onsubmit=async e=>{e.preventDefault();const login=$('#authLogin').value.trim().toLowerCase(),password=$('#authPassword').value,name=$('#authName')?.value.trim();if(!login||!password)return;const accounts=getAccounts();if(authMode==='register'){if(!name){authError='Введите имя учителя';return render()}if(accounts[login]){authError='Такой логин уже существует';return render()}accounts[login]={teacherName:name,passwordHash:await hash(password)};setAccounts(accounts);let migrated=null;try{migrated=JSON.parse(localStorage.getItem(LEGACY_KEY)||'null')}catch{};localStorage.setItem(dataKey(login),JSON.stringify(migrated?{...initial(),...migrated}:initial()));startSession(login);render()}else{const a=accounts[login];if(!a||a.passwordHash!==await hash(password)){authError='Неверный логин или пароль';return render()}startSession(login);render()}}}
function navButton(id,icon,label){return `<button class="nav-btn ${view===id?'active':''}" data-nav="${id}"><span class="nav-icon">${icon}</span><span>${label}</span></button>`}
function decorations(){return `<div class="page-decorations" aria-label="Интерактивные украшения"><button class="sparkle-btn decor-1" data-chime aria-label="Розовая звёздочка"><img src="${ETP_ASSETS['decor-star-pink.png']}" alt=""></button><button class="sparkle-btn decor-2" data-chime aria-label="Сиреневая звёздочка"><img src="${ETP_ASSETS['decor-star-lilac.png']}" alt=""></button><button class="sparkle-btn decor-3" data-chime aria-label="Голубая звёздочка"><img src="${ETP_ASSETS['decor-star-blue.png']}" alt=""></button><button class="sparkle-btn decor-4" data-chime aria-label="Мятная звёздочка"><img src="${ETP_ASSETS['decor-star-mint.png']}" alt=""></button><button class="sparkle-btn decor-5" data-chime aria-label="Мятное сердечко"><img src="${ETP_ASSETS['decor-heart-mint.png']}" alt=""></button><button class="sparkle-btn decor-6" data-chime aria-label="Розовое сердечко"><img src="${ETP_ASSETS['decor-heart-pink.png']}" alt=""></button><button class="sparkle-btn decor-7" data-chime aria-label="Сиреневое сердечко"><img src="${ETP_ASSETS['decor-heart-lilac.png']}" alt=""></button><button class="sparkle-btn decor-8" data-chime aria-label="Персиковая звёздочка"><img src="${ETP_ASSETS['decor-star-peach.png']}" alt=""></button><button class="sparkle-btn decor-9" data-chime aria-label="Голубое сердечко"><img src="${ETP_ASSETS['decor-heart-blue.png']}" alt=""></button><button class="sparkle-btn decor-10" data-chime aria-label="Жёлтое сердечко"><img src="${ETP_ASSETS['decor-heart-yellow.png']}" alt=""></button></div>`}
function hero(){return `<section class="hero"><div class="greeting">Добрый день, ${esc(account.teacherName)}!<small>Пусть день будет лёгким и вдохновляющим ✦</small></div>${mediaPanel()}<div class="clock-card"><div class="clock-date" id="clockDate">${fmtTop(currentDate())}</div><div class="clock-time" id="clockTime">--:--:--</div><div class="clock-zone">МОСКОВСКОЕ ВРЕМЯ</div></div></section>`}
function mediaPanel(){return `<div class="media-box" id="mediaBox"><div class="media-content">${mediaMarkup()}</div><div class="media-controls"><input class="media-url" id="mediaUrl" value="${esc(state.mediaUrl||'')}" placeholder="Вставьте ссылку"><button class="media-btn" data-media-link>ОК</button><label class="media-file-label">＋ Файл<input class="media-file" id="mediaFile" type="file"></label><button class="media-btn media-full" data-media-full title="На весь экран">⛶</button></div></div>`}
function mediaMarkup(){const src=mediaRuntime.url||state.mediaUrl;if(!src)return `<div class="media-placeholder"><div><div class="media-tip">Прикрепи файл или ссылку чтобы вывести на экран</div><div>Видео · PDF · изображение · презентационный файл</div></div></div>`;const type=mediaRuntime.url?mediaRuntime.type:inferType(src);if(type.startsWith('image/'))return `<img src="${esc(src)}" alt="Просмотр файла">`;if(type.startsWith('video/'))return `<video src="${esc(src)}" controls></video>`;if(type==='application/pdf')return `<object data="${esc(src)}" type="application/pdf"><div class="media-placeholder">PDF открыт для проектора</div></object>`;if(type.startsWith('audio/'))return `<audio src="${esc(src)}" controls style="width:90%"></audio>`;const embed=embedUrl(src);return `<iframe src="${esc(embed)}" allow="autoplay; fullscreen; picture-in-picture" allowfullscreen></iframe>`}
function inferType(src){const x=src.split('?')[0].toLowerCase();if(/\.(png|jpg|jpeg|webp|gif|svg)$/.test(x))return'image/*';if(/\.(mp4|webm|mov|m4v)$/.test(x))return'video/*';if(/\.pdf$/.test(x))return'application/pdf';if(/\.(mp3|wav|ogg)$/.test(x))return'audio/*';return'text/html'}
function embedUrl(url){try{const u=new URL(url);if(u.hostname.includes('youtube.com')){const id=u.searchParams.get('v');if(id)return`https://www.youtube.com/embed/${id}`}if(u.hostname==='youtu.be')return`https://www.youtube.com/embed/${u.pathname.slice(1)}`;const r=url.match(/rutube\.ru\/video\/([a-zA-Z0-9]+)/);if(r)return`https://rutube.ru/play/embed/${r[1]}`;return url}catch{return url}}
function weekView(){return `<div class="dashboard-head"><div><h1 class="section-title">Моя неделя</h1><div class="title-underline"></div></div><button class="add-lesson-3d" data-open="addLesson"><span class="plus-badge" aria-hidden="true"></span>Добавить урок</button></div><div class="dashboard"><div class="week-card"><div class="week-scroll">${weekGrid()}</div></div><div class="right-col">${ideasCard()}${planCard()}${paidTracker()}</div></div>`}
function todayView(){const now=currentDate(),lessons=state.schedule.filter(x=>x.dow===now.getDay()).sort((a,b)=>a.time.localeCompare(b.time));return `<div class="dashboard-head"><div><h1 class="section-title">Сегодня</h1><div class="title-underline"></div></div><button class="add-lesson-3d" data-open="addLesson"><span class="plus-badge"></span>Добавить урок</button></div><div class="today-view"><div class="today-list"><h3 style="font-family:Georgia,serif;font-size:26px;margin:0 0 6px">${DOW[now.getDay()]}, ${fmtShort(now)}</h3><div class="subtle">Расписание синхронизировано с разделом «Моё расписание».</div>${lessons.length?lessons.map(l=>`<div class="today-lesson"><div class="today-time">${l.time}</div><div class="today-dot" style="background:${COLORS[l.color]}"></div><div><div class="today-title">${esc(l.name)}</div><div class="today-group">${esc(l.group||'Без группы')}</div></div></div>`).join(''):`<div class="today-empty">На сегодня уроков нет. Измените «Моё расписание» — здесь данные обновятся автоматически.</div>`}</div><div class="right-col">${ideasCard()}${planCard()}${paidTracker()}</div></div>`}
function weekGrid(){const dates=weekDates(),now=currentDate();let html=`<div class="week-grid"><div class="corner"></div>`;dates.forEach((d,i)=>html+=`<div class="day-head ${dateKey(d)===dateKey(now)?'current':''}"><div class="dow">${SHORT[(i+1)%7]}</div><div class="date">${fmtShort(d)}</div></div>`);hourRows().forEach(h=>{html+=`<div class="time-cell">${String(h).padStart(2,'0')}:00</div>`;for(let dow=1;dow<=7;dow++){const colDow=dow===7?0:dow,current=dates[dow-1]&&dateKey(dates[dow-1])===dateKey(now);const lessons=state.schedule.filter(x=>x.dow===colDow&&parseInt(x.time)===h).sort((a,b)=>a.time.localeCompare(b.time));html+=`<div class="slot ${current?'current-col':''}">${lessons.slice(0,1).map(l=>`<div class="lesson-card ${l.color}"><div class="t">${esc(l.time)} – ${String(parseInt(l.time)).padStart(2,'0')}:45</div><div>${esc(l.name)}</div><div class="g">${esc(l.group)}</div></div>`).join('')}</div>`}});return html+`</div>`}
function ideasCard(){const first=state.ideas[0]||'Добавьте первую идею';return `<section class="card ideas-card"><h3 class="card-title-icon"><span>Мои блестящие идеи</span><img src="${ETP_ASSETS['ideas-bulb.png']}" alt=""></h3><div class="ideas-hero"><img src="${ETP_ASSETS['ideas-bulb.png']}" alt=""></div><div class="ideas-preview">${esc(first)}</div><button class="soft-btn" data-open="ideas">Открыть идеи <span class="count-pill">${state.ideas.length}</span></button></section>`}
function planCard(){const d=currentDate(),key=dateKey(d),items=state.plans[key]||[];return `<section class="card plan-card"><h3 class="card-title-icon"><span>План на день</span><img src="${ETP_ASSETS['plan-teacher.png']}" alt=""></h3><div class="plan-date">${fmtTop(d)}</div><div class="plan-head"><span>✓</span><span>Время</span><span>Задача</span></div><div>${items.map(planRow).join('')}</div><button class="add-task" data-add-task><span class="task-plus">+</span>Добавить дело</button></section>`}
function planRow(x,i){return `<div class="plan-row ${x.done?'is-done':''}" data-plan-row="${i}"><input class="plan-check" type="checkbox" ${x.done?'checked':''} data-plan-done><select class="plan-time" data-plan-time>${times().map(t=>`<option ${t===x.time?'selected':''}>${t}</option>`).join('')}</select><input class="plan-task ${x.done?'done':''}" data-plan-task value="${esc(x.task||'')}" placeholder="Запишите задачу"><button class="delete-mini" data-plan-del>×</button></div>`}
function paymentStatus(p){if(p.paid)return'paid';if(p.dueDate&&p.dueDate<dateKey())return'overdue';return'unpaid'}
function paymentCounts(){const all=state.payments||[];return{all:all.length,paid:all.filter(p=>paymentStatus(p)==='paid').length,unpaid:all.filter(p=>paymentStatus(p)==='unpaid').length,overdue:all.filter(p=>paymentStatus(p)==='overdue').length}}
function paidTracker(){const c=paymentCounts();return `<section class="card paid-card"><h3>Трекер платных занятий</h3><div class="paid-stats"><button class="paid-stat" data-pay-filter="all"><div class="ico">👧</div><div class="num">${c.all}</div><div class="lbl">Всего детей</div></button><button class="paid-stat" data-pay-filter="paid"><div class="ico">✓</div><div class="num">${c.paid}</div><div class="lbl">Оплачено</div></button><button class="paid-stat" data-pay-filter="unpaid"><div class="ico">○</div><div class="num">${c.unpaid}</div><div class="lbl">Не оплачено</div></button><button class="paid-stat" data-pay-filter="overdue"><div class="ico">!</div><div class="num">${c.overdue}</div><div class="lbl">Просрочено</div></button></div><button class="paid-open" data-pay-filter="all">Открыть список оплат</button></section>`}

function openModal(type,extra={}){modal={type,...extra};render()}
